[package]
name = "emu-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "What every emulated CPU shares: the bus contract, the Core trait, traps, clocks, and the AArch64 encoder"

[dependencies]

[lints]
workspace = true
