[package]
name = "esp32c6"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "ESP32-C6 SoC model: memory map, peripherals, machine"

[dependencies]
emu-core = { path = "../emu-core" }
esp-periph = { path = "../esp-periph" }
riscv-rv32 = { path = "../riscv-rv32" }
esp-soc = { path = "../esp-soc" }

[lints]
workspace = true
